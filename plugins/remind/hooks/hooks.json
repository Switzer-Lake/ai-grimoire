{
  "hooks": {
    "SessionStart": [
      {
        "matcher": "startup|clear",
        "hooks": [
          {
            "type": "command",
            "command": "python3 \"${CLAUDE_PLUGIN_ROOT}/lib/run.py\" remind hook || python \"${CLAUDE_PLUGIN_ROOT}/lib/run.py\" remind hook || exit 0",
            "timeout": 15
          }
        ]
      }
    ]
  }
}
